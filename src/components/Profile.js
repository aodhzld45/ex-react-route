import React from 'react';

function Profile({ user }) {
  //const { email, password, name } = user || {};
  return (
    <>
    
      <h1>Profile</h1>
 
    </>
  );
}

export default Profile;